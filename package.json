{
  "name": "gps-tracking-demo",
  "version": "1.1.0",
  "private": true,
  "description": "GPS Tracking — عرض محلي لأسطول مركبات في الرياض باستخدام Leaflet وOpenStreetMap.",
  "type": "commonjs",
  "scripts": {
    "dev": "node server.js",
    "start": "NODE_ENV=production node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
